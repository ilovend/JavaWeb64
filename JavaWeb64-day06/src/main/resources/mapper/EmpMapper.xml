<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.day06.mapper.EmpMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.itheima.day06.pojo.Emp">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="image" property="image"/>
        <result column="job" property="job"/>
        <result column="entrydate" property="entrydate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <sql id="s2">
        select id,
               username,
               password,
               name,
               gender,
               image,
               job,
               entrydate,
               dept_id,
               create_time,
               update_time
        from emp
    </sql>
    <insert id="add" parameterType="com.itheima.day06.pojo.Emp">
        INSERT into emp (username,
                         name,
                         gender,
                         image,
                         dept_id,
                         entrydate,
                         job,
                         create_time,
                         update_time)
        values (#{username},
                #{name},
                #{gender},
                #{image},
                #{deptId},
                #{entrydate},
                #{job},
                #{createTime},
                #{updateTime})
    </insert>
    <update id="update" parameterType="com.itheima.day06.pojo.Emp">
        update emp
        <set>
            <if test="username != null and username != ''">
               username = #{username},
            </if>
            <if test="name !=null and name != ''">
                name =#{name},
            </if>
            <if test="gender !=null and gender != ''">
                gender=#{gender},
            </if>
            <if test="image != null and image != ''">
                image= #{image},
            </if>
            <if test="deptId !=null">
                dept_id=#{deptId},
            </if>
            <if test="entrydate !=null">
                entrydate=#{entrydate},
            </if>
            <if test="job !=null">
                job =#{job},
            </if>
        </set>
        where id = #{id}
    </update>
    <delete id="deleteByIds" parameterType="java.util.ArrayList">
        DELETE from emp where id IN
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <select id="page" resultType="com.itheima.day06.pojo.Emp">
        <include refid="s2"/>
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="gender !=null">
                AND gender = #{gender}
            </if>
            <if test="begin != null and end !=null">
                AND entrydate between #{begin} and #{end}
            </if>
        </where>
        Limit #{index},#{pageSize}
    </select>
    <select id="countPage" resultType="java.lang.Long">
        select count(*) from emp
        <where>
            <if test="name != null and name != ''">
                AND name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="gender !=null">
                AND gender = #{gender}
            </if>
            <if test="begin != null and end !=null">
                AND entrydate between #{begin} and #{end}
            </if>
        </where>
    </select>
    <select id="findById" resultType="com.itheima.day06.pojo.Emp" parameterType="java.lang.Integer">
        <include refid="s2"/>
        where id = #{id}
    </select>
    <select id="findByUsername" resultType="com.itheima.day06.pojo.Emp">
        select id,
               username,
               password,
               name,
               gender,
               image,
               job,
               entrydate,
               dept_id,
               create_time,
               update_time
        from emp
        where username = #{username} and password = #{password};
    </select>
</mapper>
