<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.day04.mapper.EmpMapper">

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="com.itheima.day04.pojo.Emp">
        <id column="id" property="id"/>
        <result column="username" property="username"/>
        <result column="password" property="password"/>
        <result column="name" property="name"/>
        <result column="gender" property="gender"/>
        <result column="image" property="image"/>
        <result column="job" property="job"/>
        <result column="entrydate" property="entrydate"/>
        <result column="dept_id" property="deptId"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 通用查询列 -->
    <sql id="s1">
        select id,
               username,
               name,
               gender,
               image,
               job,
               entrydate,
               dept_id,
               create_time,
               update_time
        from emp
    </sql>
    <delete id="deleteById">
        delete from emp where id in
        <foreach collection="ids" item="id" separator="," open="(" close=")">
            #{id}
        </foreach>
    </delete>
    <select id="setCount" resultType="java.lang.Long">
        select COUNT(*) from emp
        <where>
            <if test="name!=null">
                AND name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="gender!=null">
                AND gender = #{gender}
            </if>
            <if test="begin!=null and end!=null">
                AND entrydate &gt;= #{begin} AND entrydate &lt;= #{end}
            </if>
        </where>
    </select>


    <select id="setPage" resultType="com.itheima.day04.pojo.Emp">
        <include refid="s1"></include>
        <where>
            <if test="name!=null">
                AND name LIKE CONCAT('%',#{name},'%')
            </if>
            <if test="gender!=null">
                AND gender = #{gender}
            </if>
            <if test="begin!=null and end!=null">
                AND entrydate &gt;= #{begin} AND entrydate &lt;= #{end}
            </if>
        </where>
        LIMIT #{index},#{pageSize}
    </select>


</mapper>
